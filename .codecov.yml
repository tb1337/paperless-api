codecov:
  branch: master
  notify:
    after_n_builds: 4

coverage:
  range: "95..100"

  status:
    project: no

flags:
  library:
    paths:
      - pypaperless/
  configs:
    paths:
      - ".git*"
      - "*.toml"
      - "*.yml"
  docs:
    paths:
      - docs/
      - "*.md"
  tests:
    paths:
      - tests/
